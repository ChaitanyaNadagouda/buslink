# Copy this file to infrastructure/.env and fill in real values.
# infrastructure/.env is gitignored and must never be committed.

# PostgreSQL
POSTGRES_DB=buslink
POSTGRES_USER=
POSTGRES_PASSWORD=

# pgAdmin
PGADMIN_DEFAULT_EMAIL=
PGADMIN_DEFAULT_PASSWORD=
